node_modules/
dist/
coverage/
.next/
out/
build/

# env / secrets — NEVER commit
.env
.env.*
!.env.example

# editor / os
.DS_Store
*.log
.vscode/
.idea/

# MCP memory (local context, not source)
memory.json

# turbo / cache
.turbo/
*.tsbuildinfo

# expo generated local state (types, cache) — not source
**/.expo/

# playwright
test-results/
playwright-report/
playwright/.cache/
.playwright-mcp/
.playwright-cli/
output/playwright/

.vercel

# supabase CLI local state (linked project, versions) — not source
supabase/.temp/
