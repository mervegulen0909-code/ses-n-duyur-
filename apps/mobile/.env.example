# Copy to .env and fill. Public (EXPO_PUBLIC_*) — client-safe, RLS-protected.
# These MUST match the web app's Supabase (NEXT_PUBLIC_SUPABASE_*) so mobile and
# web read the SAME data. Production = the "vocalleague" project:
#   https://twrwixownormzussyzse.supabase.co
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
